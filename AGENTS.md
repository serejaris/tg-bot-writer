# tg-bot-writer

Автоматизированное ведение Telegram-канала (контент-завод). Сейчас — только тексты и контент-план. Публикация по расписанию появится позже.

## Назначение и границы

- Этот репозиторий хранит **тексты постов** и **контент-план** для одного Telegram-канала.
- Источник правды о канале: `channel-brief.md` (аудитория, тон, темы, ограничения).
- Источник правды о постах: файлы в `posts/` и `content-plan/content-plan.md`.
- **Сейчас вне scope:** публикация в Telegram, cron, бот, деплой, правки кода автоматизации.
- Если пользователь просит «опубликовать» — напомни, что этап публикации ещё не подключён; сохрани пост как готовый черновик в `posts/scheduled/`.

## Канонические пути

| Путь | Назначение |
|------|------------|
| `channel-brief.md` | Профиль канала: для кого, о чём, как пишем |
| `content-plan/content-plan.md` | **Единственный** контент-план: даты, меню, списки покупок, статусы |
| GitHub Issues | Задачи, sub-issues, привязка коммитов (`Refs #N`) |
| `posts/drafts/` | Черновики по свежим идеям |
| `posts/scheduled/` | Готовые тексты с датой публикации |
| `posts/published/` | Архив уже вышедших постов (заполняется вручную или позже автоматически) |

## Главный сценарий: идея → пост → файл

Когда пользователь приходит с идеей («напиши пост про…», «оформи в пост…»):

1. **Прочитай** `channel-brief.md`. Если файла нет или в нём пустые секции — один раз уточни у пользователя аудиторию и тон, затем дополни brief.
2. **Уточни только критичное:** дата публикации, длина, CTA, ссылки, запретные темы. Не задавай больше 2–3 вопросов за раз.
3. **Напиши пост** на русском, в стиле канала из brief. Не выдумывай факты, цифры, цитаты и ссылки — если данных нет, спроси или пометь `[TODO: …]` в черновике.
4. **Сохрани файл** в `posts/drafts/` или `posts/scheduled/` (см. именование ниже).
5. **Обнови контент-план** — только файл `content-plan/content-plan.md`: строка с датой, статус, ссылка на файл поста.
6. **Привяжи работу к issue** — если меняешь файлы репозитория, используй подходящий issue; для новой задачи предложи создать issue.
7. **Покажи пользователю** краткое превью поста и путь к сохранённому файлу.

## Формат файла поста

Каждый пост — Markdown с YAML frontmatter:

```yaml
---
title: "Короткий заголовок для файла"
date: 2026-06-18          # планируемая дата публикации или null
status: draft             # draft | scheduled | published
topic: "одна строка темы"
tags: [ai, tools]
source: "идея пользователя / контент-план / …"
created: 2026-06-18
---

Текст поста для Telegram.

Используй **жирный**, _курсив_, списки и эмодзи по правилам из channel-brief.md.
```

После frontmatter — **только текст поста**, готовый к копированию в Telegram. Без комментариев агента, без «вот вариант» и мета-объяснений.

## Именование файлов

- Черновик: `posts/drafts/YYYY-MM-DD-<slug>.md`
- С датой публикации: `posts/scheduled/YYYY-MM-DD-<slug>.md`
- `<slug>` — латиница, kebab-case, 2–5 слов по теме: `codex-opus-update`, `weekly-digest`

Если файл с таким именем уже есть — добавь суффикс `-2`, `-3`.

## Контент-план

**Один файл:** `content-plan/content-plan.md` — единственный источник правды о плане публикаций.

- Не создавай отдельные файлы в `content-plan/` (никаких `YYYY-MM.md`, `calendar-*.md`, `menu-*.md`).
- Все изменения плана — только в `content-plan/content-plan.md`: календарь, меню, списки покупок, статусы, ссылки на посты.
- При смене периода — обновляй секции внутри того же файла, не плоди новые.

Формат строки календаря:

```md
| Дата | Нед. | Тип | Завтрак | Обед | Заметка | Статус | Файл |
| 2026-06-23 | W1 | 📅 менú | #1 Мате и тосты | #11 Чечевичное рагу | замочить чечевицу | idea | — |
```

Статусы: `idea` → `draft` → `scheduled` → `published`.

При новой идее без даты — добавь строку со статусом `idea`. При сохранении поста — обнови статус и колонку «Файл».

## Стиль и качество текста

- Весь текст — **на русском**: посты, описания, шаги, заметки, контент-план, рецепты.
- **Продукты и названия блюд** для аргентинского супера: `русский (испанский)` — испанское только в скобках, чтобы найти на прилавке.
- **Отделы магазина** в списках покупок: `Овощи и фрукты (verdulería)`, `Молочное (lácteos)` и т.п.
- Не пиши предложения на испанском. Не используй формат «русский / испанский» через слэш — только скобки.
- Обращение, длина, эмодзи, структура — строго из `channel-brief.md`.
- Telegram-разметка: `**bold**`, `_italic_`, ссылки `[текст](url)`. Не используй заголовки `#` в теле поста — они не нужны в Telegram.
- Один пост — одна мысль. Хук в первых 1–2 строках.
- Без воды, без «в современном мире», без канцелярита.
- Если пост длинный (>1500 символов) — предложи разбивку на серию или сокращение.

## Редактирование существующих постов

- Меняй только запрошенный файл; не переписывай соседние посты.
- Сохраняй frontmatter; обновляй `status` при смене стадии.
- Перед крупной правкой — прочитай 2–3 последних поста в `posts/` для一致ности тона.

## GitHub Issues

- Каждое **изменение в репозитории** (код, конфиг, тексты, правила, контент-план) должно быть привязано к **issue проекта** на GitHub.
- Перед работой: найди подходящий открытый issue (`gh issue list`) или предложи создать новый, если задачи нет.
- В issue указывай scope: что меняется и зачем. Не дублируй длинные diff — ссылка на коммит достаточна.
- Если задача является частью другой задачи, оформляй связь через GitHub **Relationships / Sub-issues**, а не текстом в теле issue.
- Не добавляй в body issue ручные строки вида «подзадача для #N», если связь можно оформить через GitHub Relationships.
- Для нового дочернего issue используй `gh issue create --parent PARENT-ISSUE-NUMBER`, если локальная версия `gh` поддерживает этот флаг.
- Если локальный `gh` не поддерживает sub-issues, создай issue обычной командой, затем добавь связь через REST API:

```bash
SUB_ISSUE_ID=$(gh api repos/OWNER/REPO/issues/SUB_ISSUE_NUMBER --jq .id)
gh api repos/OWNER/REPO/issues/PARENT_ISSUE_NUMBER/sub_issues \
  -X POST \
  -H "Accept: application/vnd.github+json" \
  -H "X-GitHub-Api-Version: 2026-03-10" \
  -F sub_issue_id="$SUB_ISSUE_ID"
```

- После создания sub-issue проверь связь через GitHub UI или API parent/sub-issues.
- По мере выполнения задачи обновляй её описание или комментарий с прогрессом: что уже сделано, что осталось сделать, какой следующий шаг.
- Для задач с несколькими шагами используй GitHub checklist (`- [ ]`, `- [x]`) и отмечай завершённые пункты сразу после выполнения.
- Перед закрытием issue обязательно сделай `git push`, чтобы связанные коммиты отразились в GitHub-задаче.
- **Коммит без issue запрещён**, кроме мелочи вроде опечаток по прямой просьбе «исправь typo» — тогда привяжи к ближайшему релевантному issue или создай issue post-factum.

### Привязка в коммите

В теле сообщения коммита — строка с номером issue:

```
Добавить скрипт публикации в Telegram.

Refs #1
```

Если issue закрывается этим коммитом — используй `Closes #1` или `Fixes #1`.

### Привязка в PR (если создаётся)

- Заголовок или описание PR содержит `Refs #N` или `Closes #N`.
- Один PR — один issue (или явно перечисленные связанные issues).

## Git и коммиты

- Сообщения коммитов пиши **на русском языке**.
- Формат: краткий заголовок (до ~72 символов), при необходимости — пустая строка и 1–2 предложения с контекстом «зачем».
- Заголовок — в повелительном наклонении или от глагола: «Добавить…», «Обновить…», «Исправить…».
- Не дублируй в сообщении список всех изменённых файлов — опиши суть.
- **Обязательно:** строка `Refs #N` (или `Closes #N`) с номером issue.
- Коммить и пушить только по явной просьбе пользователя.
- После push — при необходимости оставь комментарий в issue со ссылкой на коммит (`gh issue comment`).

## Безопасные действия по умолчанию

- Читать и писать файлы в `posts/`, `content-plan/content-plan.md`, `channel-brief.md`.
- Создавать новые посты и записи в контент-плане.
- Предлагать темы на неделю, если пользователь просит «контент-план» или «идеи на неделю».

## Требуют подтверждения

- Удаление файлов постов или строк контент-плана.
- Массовое переименование или перенос файлов между `drafts/` и `scheduled/`.
- Git commit и push (только с привязкой к issue: `Refs #N`).
- Любая публикация в Telegram или вызов внешних API.
- Изменение `channel-brief.md`, если пользователь не просил обновить профиль канала.

## Запрещено

- Публиковать посты в Telegram без явной команды пользователя.
- Хранить токены бота, API-ключи и секреты в репозитории.
- Выдумывать факты, статистику, цитаты, URL.
- Добавлять код автоматизации, CI, скрипты публикации — пока пользователь явно не перейдёт к следующему этапу.
- Коммит без `Refs #N` / `Closes #N` в сообщении.
- Создавать дополнительные файлы в `content-plan/` — только `content-plan.md`.
- Создавать лишние README и документацию без запроса.

## Контракт выполнения

**Готово**, когда:

1. Пост сохранён в файл с корректным frontmatter и именем.
2. Контент-план обновлён (если применимо).
3. Пользователь видит превью и путь к файлу.

**Спроси**, если:

- Неясно, draft это или scheduled.
- Нет данных для фактических утверждений в посте.
- Идея противоречит ограничениям из `channel-brief.md`.

## Валидация перед завершением

- Frontmatter валиден, поле `status` заполнено.
- Тело поста не содержит `#`-заголовков и мета-комментариев агента.
- Имя файла соответствует дате и slug.
- Тон согласован с `channel-brief.md` и недавними постами.
- Если менялся пайплайн — mermaid-диаграммы и описание в `README.md` актуальны.
- Перед коммитом — в сообщении есть `Refs #N` или `Closes #N`.

## Документация пайплайна (README.md)

- `README.md` — единственный файл с описанием пайплайна для людей. Диаграммы — в формате Mermaid.
- Если меняется **пайплайн** — обнови `README.md` в том же изменении:
  - новые или удалённые этапы, папки, статусы, интеграции;
  - переход к автопубликации, планировщику, боту;
  - смена главного сценария работы с постами.
- Обновляй **все затронутые mermaid-диаграммы** (пайплайн, жизненный цикл, структура репозитория), а не только одну.
- Текст вокруг диаграмм держи кратким и согласованным с `AGENTS.md` — без дублирования длинных правил.
- Не добавляй отдельные docs-файлы про пайплайн; правки — только в `README.md` и `AGENTS.md`.
- Если изменение чисто текстовое (один пост, правка brief) — README не трогай.

## Следующий этап (не делать без команды)

Когда пользователь скажет перейти к автоматизации:

- Планировщик ежедневной публикации.
- Интеграция с Telegram Bot API.
- Перенос постов из `posts/scheduled/` в канал.

До этой команды оставайся в режиме **только тексты**.
